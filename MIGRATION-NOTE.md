# My Wealth Square website migration

Target public URL: https://lbp1122.github.io/my-wealth-square/

The website content uses relative links, so renaming the repository from `balance-sheet-square` to `my-wealth-square` will preserve the internal Privacy, Support, CSS and JavaScript paths.

The Android package ID remains `com.lbp.balancesheetsquare` and must not be changed.
